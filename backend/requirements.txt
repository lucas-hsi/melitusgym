# Core FastAPI dependencies with binary wheels
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlmodel==0.0.22

# Database dependencies (binary wheels available)
psycopg2-binary==2.9.10
alembic==1.14.0

# Authentication & Security (FIXED: specific versions with guaranteed wheels)
# Avoid cryptography compilation issues
cryptography==41.0.8
python-jose==3.3.0
# Avoid bcrypt compilation issues  
bcrypt==4.1.3
passlib==1.7.4

# HTTP & Validation (latest stable with wheels)
httpx==0.28.1
python-multipart==0.0.12
python-dotenv==1.0.1
email-validator==2.2.0

# Pydantic ecosystem (latest stable)
pydantic==2.10.3
pydantic-settings==2.7.0

# Utilities with binary wheels
tenacity==9.0.0

# Firebase Cloud Messaging dependencies (with versions)
aiohttp==3.11.10
google-auth==2.37.0

# Additional optimization flags
# Install with: pip install --prefer-binary --no-cache-dir -r requirements.txt