# RENDER COMPATIBLE - Python 3.11 with guaranteed binary wheels
# Core FastAPI dependencies (older stable versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlmodel==0.0.22

# Database dependencies (binary wheels available)
psycopg2-binary==2.9.9
alembic==1.12.1

# Authentication & Security (FIXED: older versions with guaranteed wheels)
# Avoid cryptography compilation issues
cryptography==41.0.7
python-jose==3.3.0
# Avoid bcrypt compilation issues  
bcrypt==4.0.1
passlib==1.7.4

# HTTP & Validation (older stable with wheels)
httpx==0.25.2
python-multipart==0.0.6
python-dotenv==1.0.0
email-validator==2.1.0

# Pydantic ecosystem (older stable - CRITICAL: avoid pydantic-core Rust builds)
pydantic==2.5.0
pydantic-settings==2.1.0

# Utilities with binary wheels
tenacity==8.2.3

# Firebase Cloud Messaging dependencies (older versions)
aiohttp==3.9.1
google-auth==2.25.2

# Additional optimization flags
# Install with: pip install --prefer-binary --no-cache-dir -r requirements.txt