from typing import Optional

# Stub leve para evitar erro de import durante o boot.
# Quando o agendador real estiver pronto, substituir por implementação completa.

async def start_fcm_alarm_scheduler(enabled: Optional[bool] = False) -> None:
    """
    Inicia o agendador de alarmes FCM se habilitado.
    No stub, apenas registra uma execução rápida e retorna.
    """
    # Evita bloquear o startup do backend quando não configurado.
    return